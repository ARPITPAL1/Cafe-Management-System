{
  "name": "frontend-fallback",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"const { execSync } = require('child_process'); execSync('npm run build', { cwd: '..', stdio: 'inherit' }); const fs = require('fs'); if (fs.existsSync('../dist')) { fs.cpSync('../dist', './dist', { recursive: true }); }\""
  }
}
